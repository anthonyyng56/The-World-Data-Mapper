import React from 'react';
import { Link } from 'react-router-dom'

const Logo = (props) => {
    return (
        <div className='logo'>
            The World Data Mapper
        </div>
    );
};

export default Logo;